start-with-ip.js

This script detects your machine's local IPv4 address and starts Expo with NEXT_PUBLIC_API_URL set to http://<your-ip>:8000.

Usage:
  npm run start:ip

This is useful when testing the app on a physical device or emulator and you want the mobile app to reach a backend running on your machine.
